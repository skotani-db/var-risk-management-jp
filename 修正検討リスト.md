# ハードコード修正検討リスト

> 作成日: 2026-06-03  
> 対象: `var-risk-management-jp/` 配下の全ノートブック（00〜11）+ config + lakeflow

---

## 方針

`config/configure_notebook.py` が唯一の設定定義箇所。  
各ノートブックは `%run ./config/configure_notebook` で config dict を取得し、  
カタログ名・スキーマ名・テーブル名はすべて config 経由で参照すべき。

---

## 修正検討リストへの対応方針

### ゴール

**`config/configure_notebook.py` の `catalog` と `schema` を1箇所変更するだけで、全ノートブック＋パイプラインが別環境で動作すること。**

### 現状分析

```
configure_notebook.py
  ├─ config['database']['catalog'] = 'takuyaa_azure_w2'
  ├─ config['database']['schema']  = 'workshop_var'
  ├─ USE CATALOG {catalog}    ← SQL デフォルトカタログ設定
  └─ USE SCHEMA {schema}      ← SQL デフォルトスキーマ設定
```

`USE CATALOG` / `USE SCHEMA` を実行済みのため、SQL セル内の非修飾テーブル名（例: `market_data`）は  
**カタログ・スキーマの変更に対して既にポータブル**。  
問題は以下の2種類のみ：

1. **完全修飾パスのハードコード**（分類A）— カタログ/スキーマ名がリテラルで埋め込まれている
2. **スキーマ名をSQL内で明示参照している箇所**（分類A #3, 分類B #5）

### 対応の優先度と方針

| 優先度 | 対象 | 方針 | 理由 |
|---|---|---|---|
| **P0** | 分類A (3件) | 環境変数 + パラメータ注入 | カタログ/スキーマ変更時に壊れる |
| **P1** | 分類B のうちスキーマ名明示 (#5) | Python セルに変換 | 分類A #3 と同一箇所 |
| **P2** | 分類B のうちテーブル名 (#4〜#20) | **対応不要（現状維持）** | `USE SCHEMA` により既にポータブル |
| **P3** | 分類C (5件) | Markdown 内の値を現行設定に合わせる | 混乱防止（任意） |
| **P4** | 分類E (1件) | `fig.show()` → `display(fig)` に変更 | iframe ブロック環境で描画不可 |

### P0: 分類A の具体的対応

#### A-1: `config/configure_notebook.py` — 環境変数フォールバック

```python
import os

config = {
  'database': {
    # 環境変数があれば優先、なければデフォルト値
    'catalog': os.environ.get('VAR_CATALOG', 'takuyaa_azure_w2'),
    'schema':  os.environ.get('VAR_SCHEMA', 'workshop_var'),
    'volume':  os.environ.get('VAR_VOLUME', 'raw_data'),
    'volume_adjustments': os.environ.get('VAR_VOLUME_ADJ', 'risk_adjustments'),
    'tables': {
      'stocks': 'market_data',
      'indicators': 'market_indicators',
      ...
    },
  },
}
```

**利用方法**:
- ノートブック直接実行 → デフォルト値で動作（変更不要）
- 別環境で実行 → ジョブのタスク設定 or クラスター環境変数で `VAR_CATALOG=other_catalog` を指定

#### A-2: `lakeflow/dp_pipeline.py` — パイプラインパラメータ化

```python
# SDP パイプラインでは spark.conf からパイプライン設定を取得
catalog = spark.conf.get("pipeline.catalog", "takuyaa_azure_w2")
schema  = spark.conf.get("pipeline.schema", "workshop_var")
volume  = spark.conf.get("pipeline.volume", "raw_data")

VOLUME_PATH = f"/Volumes/{catalog}/{schema}/{volume}"
```

パイプライン設定の `configuration` に以下を追加：
```json
{
  "pipeline.catalog": "${VAR_CATALOG}",
  "pipeline.schema": "${VAR_SCHEMA}",
  "pipeline.volume": "${VAR_VOLUME}"
}
```

#### A-3: `04_unity_catalog_governance` Cell 8 — Python セルに変換

```python
# Before: %sql セル
# SHOW GRANTS ON SCHEMA workshop_var

# After: Python セル
display(sql(f"SHOW GRANTS ON SCHEMA {config['database']['schema']}"))
```

### P2: 分類B が対応不要である理由

| 条件 | 現状 |
|---|---|
| `USE CATALOG` 実行済み？ | ✓ `configure_notebook` で設定 |
| `USE SCHEMA` 実行済み？ | ✓ `configure_notebook` で設定 |
| SQL 内は非修飾名？ | ✓ `market_data`（`catalog.schema.market_data` ではない） |
| カタログ/スキーマ変更で壊れる？ | **いいえ** — `USE SCHEMA` が追従する |
| テーブル名変更で壊れる？ | はい — ただしテーブル名変更は想定外 |

**結論**: 分類B の SQL セルは `USE SCHEMA` により既にカタログ/スキーマポータブル。  
テーブル名のリネームは本ワークショップでは想定しないため、可読性優先で SQL セルのまま維持。

### P3: 分類C — Markdown の更新（任意）

説明文中の旧カタログ/スキーマ名を現行値に揃える。  
実行には影響しないが、利用者の混乱を防ぐため更新推奨。

| # | ファイル | 変更内容 |
|---|---|---|
| 21 | `00_introduction` | `shotkotani_demo_ws` → 環境変数の説明に書き換え |
| 22 | `00_introduction` | `var_risk_demo` → `workshop_var` |
| 23 | `04_unity_catalog_governance` Cell 9 | `var_risk_demo` → `workshop_var` |
| 24 | `11_lakeflow_designer_risk_report` | `var_risk_demo` → `workshop_var` |
| 25 | `lakeflow/dp_pipeline.py` Cell 1 | `var_risk_demo` → `workshop_var` |

### P4: 分類E — displayHTML / iframe 問題の対応

`databricksusercontent.com` へのネットワーク通信がブロックされている顧客環境では、  
`displayHTML` 経由の描画（Plotly `fig.show()` 含む）が失敗する。  
`display(fig)` は Databricks ネイティブレンダリングを使用し、iframe 不要。

```python
# Before
fig.show()

# After
display(fig)
```

---

## 分類A: 環境固有値（カタログ名・スキーマ名・Volumeパス）— 要修正

| # | ファイル | セル/行 | ハードコード内容 | 修正方針 |
|---|---|---|---|---|
| 1 | `config/configure_notebook.py` | L56-57 | `'catalog': 'takuyaa_azure_w2'`, `'schema': 'workshop_var'` | **環境変数対応済み** |
| 2 | `lakeflow/dp_pipeline.py` | Cell 2 | `VOLUME_PATH = "/Volumes/takuyaa_azure_w2/workshop_var/raw_data"` | **spark.conf 対応済み** |
| 3 | `04_unity_catalog_governance` | Cell 8 (SQL) | `SHOW GRANTS ON SCHEMA workshop_var` | Python セルで `sql(f"SHOW GRANTS ON SCHEMA {config[...]}")` に変更 |

---

## 分類B: テーブル名の直接SQL参照（USE SCHEMA 設定後の相対名）— 対応不要

`configure_notebook` で `USE CATALOG` + `USE SCHEMA` を実行済みのため、  
SQL セル内の `market_data` 等はスキーマ相対名として正常に動作する。  
**カタログ・スキーマの変更に対して既にポータブルであり、修正不要。**

### 04_unity_catalog_governance

| # | セル | SQL内容 | config対応キー |
|---|---|---|---|
| 4 | Cell 6 | `DESCRIBE EXTENDED market_data` | `tables.stocks` |
| 5 | Cell 8 | `SHOW GRANTS ON SCHEMA workshop_var` | `database.schema` → **分類A #3 で対応** |
| 6 | Cell 12 | `ALTER TABLE market_data SET TAGS (...)` | `tables.stocks` |
| 7 | Cell 13 | `ALTER TABLE market_indicators SET TAGS (...)` | `tables.indicators` |
| 8 | Cell 15 | `COMMENT ON TABLE market_data / market_indicators` | `tables.stocks` / `tables.indicators` |
| 9 | Cell 17 | `DESCRIBE HISTORY market_data LIMIT 5` | `tables.stocks` |
| 10 | Cell 19 | `SELECT ... FROM market_data` | `tables.stocks` |
| 11 | Cell 20 | `INSERT INTO market_data` | `tables.stocks` |
| 12 | Cell 21 | `UPDATE market_data` | `tables.stocks` |
| 13 | Cell 22 | `DESCRIBE HISTORY market_data` | `tables.stocks` |
| 14 | Cell 23 | `SELECT ... FROM market_data WHERE ticker = 'TEST'` | `tables.stocks` |
| 15 | Cell 24 | `FROM market_data VERSION AS OF 1` | `tables.stocks` |
| 16 | Cell 25 | `FROM market_data VERSION AS OF 0` | `tables.stocks` |
| 17 | Cell 26 | `DELETE FROM market_data WHERE ticker = 'TEST'` | `tables.stocks` |

### 01_data_upload_and_volume

| # | セル | SQL内容 | config対応キー |
|---|---|---|---|
| 18 | Cell 21 (SQL) | `FROM market_data` | `tables.stocks` |

### 09_dashboard_and_genie

| # | セル | SQL内容 | config対応キー |
|---|---|---|---|
| 19 | Cell 3 (SQL) | `CREATE OR REPLACE VIEW v_daily_risk_summary ... FROM market_data s` | `tables.stocks` |
| 20 | Cell 10 (SQL) | `CREATE OR REPLACE VIEW ... FROM market_data s` | `tables.stocks` |

---

## 分類C: Markdown/コメント内の参照（実行に影響なし）— 任意修正

| # | ファイル | 内容 |
|---|---|---|
| 21 | `00_introduction` | `'catalog': 'shotkotani_demo_ws'`（設定変更手順の説明文） |
| 22 | `00_introduction` | `'schema': 'var_risk_demo'`（設定変更手順の説明文） |
| 23 | `04_unity_catalog_governance` Cell 9 (MD) | `GRANT ... ON SCHEMA var_risk_demo TO auditors`（例示コード） |
| 24 | `11_lakeflow_designer_risk_report` (MD) | `スキーマ var_risk_demo`（UI手順説明） |
| 25 | `lakeflow/dp_pipeline.py` Cell 1 (MD) | `ターゲットスキーマ: var_risk_demo`（手順説明） |

---

## 分類D: システムテーブル参照（変更不要）

| # | ファイル | 内容 |
|---|---|---|
| 26 | `10_operations_system_tables` | `system.billing.usage`, `system.compute.clusters`, `system.lakeflow.pipeline_event_log` |

---

## 分類E: displayHTML / iframe 通信問題 — 顧客環境で要対応

`displayHTML()` は出力を `databricksusercontent.com` ドメインの iframe 内でレンダリングする。  
企業ネットワーク（プロキシ/ファイアウォール/NSG）がこのドメインをブロックしている環境では  
「iframe cannot reach ...」エラーが発生する。

### 影響箇所

| # | ファイル | 関数/行 | 描画方法 | iframe 依存 | 修正方針 |
|---|---|---|---|---|---|
| 27 | `utils/var_viz.py` | `plot_candlesticks()` L19 | Plotly `fig.show()` | **あり** | **display(fig) 対応済み** |
| 28 | `utils/var_viz.py` | `plot_var()` L42-58 | matplotlib | なし（PNG描画） | 対応不要 |
| 29 | `utils/var_viz.py` | `plot_correlation_heatmap()` L67-83 | matplotlib | なし（PNG描画） | 対応不要 |

### 根本原因

Plotly の `fig.show()` は Databricks 上で内部的に `displayHTML(fig.to_html())` を呼び出す。  
`displayHTML` は sandboxed iframe (`databricksusercontent.com`) を使用するため、  
ネットワークポリシーで当該ドメインがブロックされていると描画に失敗する。

### 対応方法

**方法1: コード修正（推奨）** — iframe を使わない `display()` に置き換え

```python
# utils/var_viz.py L19
# Before
fig.show()

# After
display(fig)  # Databricks ネイティブレンダリング（iframe 不要）
```

**方法2: ネットワーク許可リスト追加** — インフラ側対応

`*.databricksusercontent.com` をファイアウォール/プロキシの allowlist に追加。  
ワークスペース管理者またはネットワーク管理者に依頼が必要。

**方法3: ブラウザ側確認**

広告ブロッカーやセキュリティ拡張機能が iframe を阻害していないか確認。

---

## 修正済みの項目（2026-06-03 ジョブ実行時に対応）

| 修正内容 | ファイル |
|---|---|
| `shotkotani_demo_ws.var_risk_demo.market_data` → `market_data` | `01_data_upload_and_volume` Cell 21 |
| `%run ./config/configure_notebook` 追加 | `03_lakeflow_data_quality` |
| `SHOW GRANTS ON SCHEMA var_risk_demo` → `workshop_var` | `04_unity_catalog_governance` Cell 8 |
| `'data_classification' = 'pii'` → `'confidential'` | `04_unity_catalog_governance` Cell 12, 13 |
| `VOLUME_PATH` を正しいカタログ/スキーマに変更 | `lakeflow/dp_pipeline.py` Cell 2 |
| `catalog` / `schema` / `volume` / `volume_adjustments` を環境変数対応化 | `config/configure_notebook` |
| `VOLUME_PATH` を `spark.conf.get(...)` ベースに変更 | `lakeflow/dp_pipeline.py` Cell 2 |
| `fig.show()` → `display(fig)` | `utils/var_viz.py` |
